# Production base. The deploy script adds either the FrankenPHP or PHP-FPM runtime file.
services:
  mysql:
    restart: always

  redis:
    restart: always

volumes:
  mysql_data:
  redis_data:
  uploads:
  caddy_data:
  caddy_config:
